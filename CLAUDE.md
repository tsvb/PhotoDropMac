# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What this is

A native macOS (SwiftUI, Swift 6) app that ingests photos from removable memory cards into a date-organized library. It scans a card, previews a year/day folder tree, then copies each photo bundle with streaming hash verification, content-based deduplication, optional multi-destination archival (primary + any number of mirror copies), and optional card eject.

The discovery, dedup, companion-classification, and path-planning rules are **deliberate and load-bearing** — data safety depends on them, and several encode non-obvious decisions (same-directory-only companion matching, content-based dedup across the whole destination root, all-or-nothing bundles). Each such rule is documented at its definition; change those behaviors carefully and on purpose, not as a drive-by "fix." They stand on their own merits — judge a proposed change against what it does to data safety here, not against how any other tool behaves.

## Build & run

The `.xcodeproj` is **gitignored and generated** — it is not in version control. You must regenerate it before building a fresh checkout, and after any change to `project.yml` or after adding/removing/renaming source files:

```bash
xcodegen generate                                                      # regenerate PhotoDropMac.xcodeproj from project.yml
xcodebuild -project PhotoDropMac.xcodeproj -scheme PhotoDropMac \
           -configuration Debug -destination 'platform=macOS' build    # build
```

`xcodegen` is installed via Homebrew (`brew install xcodegen`). Deployment target is macOS 14.0. There are two targets: the `PhotoDropMac` app (scheme `PhotoDropMac`), and `photodrop`, a headless command-line tool (scheme `photodrop`).

### The `photodrop` CLI

`photodrop` is a `tool` target that compiles the shared `Core/` sources (the engines + pure logic) plus `Sources/PhotoDropCLI/`, and links **swift-argument-parser** (declared under `packages:` in `project.yml`). It never compiles the SwiftUI `UI/`. Build it with a **scheme** (a `-target` build mishandles the SPM module under Swift 6 explicit modules):

```bash
xcodegen generate
xcodebuild -project PhotoDropMac.xcodeproj -scheme photodrop \
           -configuration Debug -destination 'platform=macOS' build
```

**Exit codes across all four commands: `0` success, `1` issues found, `2` could-not-check / error, and `64` for a usage error** (an unknown flag or a missing required option — ArgumentParser's `EX_USAGE`, which the tool never chooses but always inherits; anything branching on `$?` sees it).

`--version` reads `CFBundleShortVersionString` from the bundle rather than a literal: the literal had drifted two releases behind `MARKETING_VERSION`, so a 0.2.1 build reported 0.1.3 — and a binary that misreports itself undermines the manifest it just signed, because a support conversation cannot then establish which code produced a given receipt. `photodrop verify <library|manifest.json> [--json] [--xattr]` re-verifies a library (exit `0` all-verified, `1` issues found, `2` no manifest / unreadable target / error), driving the same `VerifyEngine` the app uses. `--xattr` verifies by each file's embedded checksum attribute instead of the manifest (see below), so it works on any folder even after a reorg; it returns a `XattrOutcome` rather than a bare report because **"couldn't read the target" must never be reported as "nothing stamped"** — collapsing the two made a typo'd path exit `0` with a reassuring message forever. `photodrop ingest --from <card> --to <primary> [--archive …] [--preset …] [--layout …] [--[no-]year-folder] [--[no-]verify] [--[no-]eject] [--description …] [--folder-template …] [--file-template …] [--post-ingest-hook <path>]` drives `IngestEngine` headlessly. `photodrop layouts` lists the built-in folder layouts with the sample path each produces, rendered by the same code the engine uses so it cannot advertise a shape the ingest wouldn't build; precedence is explicit template option → `--layout` → `--preset` → default. **SIGINT, SIGTERM and SIGHUP are all graceful cancels** — each stops the job at the next file boundary so the manifest still gets written (see the copy engine). Only SIGINT was handled, which left the other three routes to a truncated file with no manifest entry and no xattr — invisible to both verify modes — reachable by closing the terminal window, `pkill`, a launchd exit timeout, or logging out. **A second signal exits immediately (130)**, and the default dispositions are restored as soon as the copy ends — before the post-ingest hook — so a blocking hook can't leave the process un-interruptible. `--to` must already exist: `FileCopier` creates intermediate directories, so a typo'd destination used to materialize a whole new library tree and exit 0 with a manifest attesting to it. The hook is an explicit option because a CLI's `UserDefaults` domain isn't the app's. `photodrop sync <library> --to <mirror> [--no-verify] [--json]` ([SyncEngine.swift](Sources/PhotoDropMac/Core/SyncEngine.swift)) brings a mirror that was not mounted at ingest time up to date — the travel case the 3-2-1 feature exists for, which previously had **no route back**: `heal` is report-only and treats recorded mirrors as recovery *sources*, so it offered the lagging NAS as a place to restore *from*; `verify` only reports; and re-running the ingest needs a card that is back in the camera. It derives what should exist from `VerifyEngine.build`, so the containment rule, the conflict rule and the mirror gate are inherited rather than re-implemented, and it writes the mirror its own manifest so the mirror becomes verifiable in its own right. **It only ever adds**: a file already at the mirror is verified and left alone, and a mismatch is *reported, never overwritten* — two disagreeing copies is exactly where picking a winner automatically kills the good one. The mirror must already exist, for the same reason `--to` must. Exit `0` up to date, `1` issues found, `2` refused. `photodrop heal <library> [--json] [--script <path>] [--mirror <path>…]` ([HealEngine.swift](Sources/PhotoDropMac/Core/HealEngine.swift)) is **report-only**: it verifies the library and, for every damaged/missing file, reports whether a healthy copy exists in a recorded mirror — and where. It **never writes to the library** — including its own output: a `--script` path *inside* the target is refused, which nothing enforced, so `--script "<lib>/PhotoDrop Manifests/x.sh"` dropped a mode-0755 shell script into the folder holding the library's integrity records. The script is created with `O_EXCL` so it can't clobber an existing file (the one write the command makes — it refuses rather than silently choosing another path), and **a failed script write never changes the verdict about the library**: it used to `throw ExitCode(2)`, so a damaged library whose script path already existed reported "could not verify" instead of "issues found", collapsing the exact distinction `ScheduledVerification` branches on. Mirror lookup uses the manifest's `destinations` array (all roots written that job; falls back to `[primaryDestination] + archiveDestination` for older manifests) — but **a recorded root is only searched if it carries a `PhotoDrop Manifests/` folder, or the user names it with `--mirror`**; see the mirror gate under the manifest trust boundary. Roots that are refused are reported in both output modes.

### Tests

There is a `PhotoDropMacTests` XCTest target (declared in `project.yml`, sources in `Tests/PhotoDropMacTests/`, wired into the `PhotoDropMac` scheme). Run it with:

```bash
xcodegen generate
xcodebuild -project PhotoDropMac.xcodeproj -scheme PhotoDropMac \
           -configuration Debug -destination 'platform=macOS' test
```

Every test file carries a `///` header comment stating the threat model and the measured before-state it pins, so `ls Tests/PhotoDropMacTests/` plus those headers is the current inventory — keep it there rather than restating it here, where it goes stale silently.

**The suite is hermetic, and must stay that way.** `Copier.hermetic(in:)` is the factory tests use: it injects the cache and index stores, the **log directory**, an isolated `UserDefaults` suite, and disables notifications. Anything reading preferences from the engine or controller must take that injected `defaults` — `CardHistory` does — and a test needing its own suite must tear it down **by name** (`removePersistentDomain(forName:)`), because the suite object itself cannot be captured into the main-actor teardown closure under strict concurrency. Each of those was a real side effect the suite fired on every integration test — measured, one run added **18 files to the real `~/Library/Logs/PhotoDrop`**, which is the user's audit trail for what happened to their photos, and `Copier` reads `UserDefaults.standard` for a post-ingest script it would then *execute*. CI fails the build if anything lands in that folder. Don't add a new side effect without an injection point.

Timeouts **fail**, they never `XCTSkip` — `xcodebuild` prints `** TEST SUCCEEDED **` over a skip, so a skip-on-timeout let the rollback regression stop running silently. (The two `XCTSkip`s in `XattrTests` are deliberate environment guards and are fine.)

Tests are **hosted** (the bundle loads into the app), so a CI runner needs a GUI session to launch the host — GitHub's macOS runners provide one.

**`HasherTests` is what pins the digests to XXH64**, against known-answer vectors from `xxh64sum` (the table lives Debug-only in [Hasher.swift](Sources/PhotoDropMac/Core/Hasher.swift) as `XxHash64Vectors.vectors`, with the test as its only consumer). It also checks every split offset of a multi-stripe message, many chunk sizes, a zero-length update mid-stream, single-bit sensitivity at every offset, and `hash(fileAt:)` against the in-memory digest either side of the 1 MiB read boundary. This matters because **every other test in the suite uses the hasher as a self-consistent oracle** — hash the source, hash the destination, compare — which passes identically if the algorithm is wrong. The previous mechanism, an `assert`-based `xxHash64SelfCheck()`, had zero callers and so ran never.

### CI

[.github/workflows/ci.yml](.github/workflows/ci.yml) runs on every push and PR: `xcodegen`, the
full suite, the `photodrop` scheme, an assertion that the suite left nothing in
`~/Library/Logs/PhotoDrop`, and [scripts/check-doc-links.sh](scripts/check-doc-links.sh) — which
exists because `CLAUDE.md` accumulated 13 broken source links just by surviving the `Core/`+`UI/`
reorganisation, and every one silently sent a reader to a file that wasn't there.

`scripts/release.sh` **runs the suite before archiving** (`SKIP_TESTS=1` overrides, loudly),
refuses a dirty tree, writes `MARKETING_VERSION` back to `project.yml`, and creates an annotated
`v<version>` tag — none of which it did before, so a notarized DMG could be built from untested
code at a version the repo never recorded. It stages a *copy* of the app for `create-dmg`, which
deletes `.DS_Store` inside its source folder — by that point the bundle is stapled, and touching
it would invalidate the signature after the ticket was attached.

### App icon

`Sources/PhotoDropMac/Assets.xcassets/AppIcon.appiconset` is **generated** by [scripts/make-icon.swift](scripts/make-icon.swift) — edit that script and re-run `swift scripts/make-icon.swift`, don't hand-patch the PNGs. It draws to Apple's macOS grid (824×824 body inside a 1024×1024 canvas, 185.4pt continuous corner, rendered from a `CALayer` with `cornerCurve = .continuous` so the curve is Apple's actual squircle). The 100pt margin is reserved for the shadow and for optical parity with other apps — don't fill it. Artwork differs by size on purpose: card + stripes at ≥64px, arrow alone at ≤32px, because detail that reads at 512px turns to mush at 16px.

### Signing / sandbox

Always **unsandboxed** (`com.apple.security.app-sandbox = false`): the app needs unrestricted filesystem access (arbitrary card → arbitrary destination) and shells out to `/usr/sbin/diskutil` via `Process` to eject cards. Don't add the sandbox entitlement without rethinking eject and folder access — and note it would also rule out the Mac App Store, so distribution is Developer ID + notarization instead.

**Accepted risk of that exemption: ImageIO decodes card bytes in-process.** `ExifReader.dateTaken` runs for every primary on every scan and `ThumbnailLoader` decodes previews, both on attacker-authored files, with the user's full filesystem access and no containment. A crafted RAW hitting a CoreGraphics decoder bug could write `~/Library/LaunchAgents` or rewrite `photodrop.postIngestScript` — which the app then executes. Hardened runtime limits code injection and gives no filesystem containment. Fixing it needs an XPC decode helper, or the sandbox; this is recorded, not fixed. **Reconsider it first if the sandbox decision is ever revisited.** It is now also stated publicly in [SECURITY.md](SECURITY.md), with a private disclosure route — a documented, exploitable-in-principle condition with no channel to report it left a finder the choice of a public issue or silence.

Signing is **per-config** ([project.yml](project.yml)): **Debug** is ad-hoc (`CODE_SIGN_IDENTITY = "-"`), hardened runtime off — fast local builds, no certificate. **Release** is **Developer ID Application**, hardened runtime **on**, `OTHER_CODE_SIGN_FLAGS = --timestamp` (what notarization requires). Hardened runtime is orthogonal to the sandbox: every runtime behavior (diskutil eject, the `launchctl` scheduled-verify agent, `PostIngestHook`, `UNUserNotificationCenter`, `setxattr`) works under it with **no extra entitlements** — `PhotoDropMac.entitlements` stays sandbox-false and unchanged.

The `photodrop` CLI is **embedded in the app** at `Contents/MacOS/photodrop` (xcodegen `dependencies: [{target: photodrop, link: false, embed: true}]`), signed on copy as part of the app's signature and covered by the same notarization. `EmbeddedCLI` ([EmbeddedCLI.swift](Sources/PhotoDropMac/UI/EmbeddedCLI.swift)) locates it via `Bundle.main`, and `MaintenancePreferences` defaults the scheduled-verify `binaryPath` to it (typed path still overrides). Releases are cut by [scripts/release.sh](scripts/release.sh) (archive → notarize + staple the **app** → DMG → notarize + staple the **DMG**); see [RELEASING.md](RELEASING.md) for prerequisites (paid Apple Developer Program + Developer ID cert + notarytool keychain profile).

## The ingest pipeline (big picture)

Data flows through a chain of mostly-pure value-type transforms, driven by three `@MainActor @Observable` controllers. End to end:

1. **`DriveWatcher`** — observes `NSWorkspace` mount/unmount and exposes `[DetectedDrive]`. **A user-chosen folder is also a `DetectedDrive`** (`DetectedDrive.folder(at:)`, `kind == .folder`): the engine never cared about the difference — the CLI has always documented `--from` as "memory card *or source folder*" — and only the GUI was fenced in, so you could not re-file an existing shoot, ingest a card someone had already dumped to a drive, re-run an ingest after the card was pulled, or evaluate the app on your own library. A folder is **never ejected** (`isEjectable`), because `DriveEjector` unmounts the *volume* the path is on, which for a folder in the user's own Pictures folder is the startup disk. Folder sources live in `MainView` state and are deliberately not persisted: a stale remembered folder in the sidebar is an invitation to re-ingest something by accident. (removable/ejectable local volumes; built-in SD readers report "internal" so that flag is intentionally *not* filtered on). **Mounted disk images are excluded** ([`VolumeBacking`](Sources/PhotoDropMac/UI/DriveWatcher.swift)): measured, a read/write HFS+ `.dmg`, a read-only UDZO `.dmg`, an APFS `.dmg` and a `.sparsebundle` all report `isLocal && isEjectable && isRemovable` — the exact signature of a card — so every image the user had open (an app installer, a backup, a downloaded ISO) was listed as a card, auto-opened the window on mount, and was a legal one-click-ingest target. The flags can't tell them apart; the DiskArbitration description can, via three OR'd signals (device model `Disk Image`, device protocol `Virtual Interface`, and the structural one — media served by the kernel's `IOHDIXController`), none of which a USB or built-in SD reader reports. It **fails open**: no readable description means "keep it", because hiding a real card is the worse error. It is injected via `.environment` and, crucially, is hosted by the `MenuBarExtra` label so it stays alive in the scene graph even when the main window is closed — that's what lets a card insertion auto-open the window ([PhotoDropMacApp.swift](Sources/PhotoDropMac/UI/PhotoDropMacApp.swift)).
2. **`AssetDiscovery.scanOutcome(root:)`** — two-pass directory walk producing a `ScanOutcome`. Pass 1: RAW primaries + their same-directory companions; pass 2: standalone stills and **video** not already claimed as a RAW's JPEG-pair. Extension sets (RAW list, sidecar `.xmp`/`.dop`/`.pp3`/`.wav`, stills, video) and the companion-classification rules (cases A/B in `classifyCompanion`) are a deliberate, fixed set. **HEIF (`heic`/`heif`/`hif`) counts as a still and video (`mov`/`mp4`/`m4v`/`avi`/`mts`/`m2ts`/…) is a primary in its own bundle**; both were dropped by the extension filter with no tally, so a hybrid card — or any iPhone or HEIF-mode Canon/Sony card — planned only what it recognized, passed the `filesFailed == 0` eject gate, wrote a manifest attesting to completeness, and ejected. Video takes the same sidecars but is **never** a companion: a clip beside a RAW of the same stem is its own shot, not the RAW's pair. Multi-file clip containers (spanned MXF/BRAW/R3D folders, `.insv` pairs) are deliberately *not* included — they are directory-shaped and need a different atomic unit than `AssetBundle`, so they are counted as unrecognized rather than half-copied. Companion matching is **same-directory only** by design, matches the neighbour's **stem exactly** (never by prefix — `hasPrefix` accepted `IMG_1234.v2.xmp` as a short-form sidecar of `IMG_1234.CR2`, and since `CopyPlan` renames short-form sidecars to `{newStem}.xmp` the two then planned onto one path), and a companion belongs to **exactly one** primary. That last rule needs `buildBundle` to honour `consumed`: with DNG Converter output — `IMG_1234.CR2`, `IMG_1234.DNG`, `IMG_1234.xmp` side by side, two RAW primaries neither of which is the other's companion — the xmp joined both bundles, so the second collided with the first on the sidecar and was pushed to `_1` while the same file was copied, hashed and manifested twice. Siblings are sorted for the same reason directories are: enumerator order otherwise decided which primary won.

   **`ScanOutcome` also counts the files it recognizes and will not copy** (`unrecognizedFiles`), excluding camera and filesystem bookkeeping (`.CTG`, `.THM`, `INDEX.BDM`, dotfiles — see `ignorableExtensions`, which exists so the warning doesn't fire on every card and stop meaning anything). That count makes `isComplete` false, which suppresses the auto-eject in both front ends: a card still holding files this ingest will not take has not been fully taken, and ejecting is the app's own "this card is finished" gesture — the next thing that happens to a finished card is a format.

   **`ScanOutcome` distinguishes "couldn't read the card" from "the card has no photos"**, and counts directories the walk could not open — the same rule `XattrOutcome` enforces on the verify side, applied at the sink that was missed. `scan(root:)` used to answer `[]` for a nonexistent, unmounted or unreadable root, so `photodrop ingest --from /Volumes/CRAD` printed *No recognized photos found* and exited **0**; a wrapper reading `$?` released the card. Within a readable card the enumerator had no error handler, so a directory returning EIO just yielded fewer bundles: a 500-photo card could plan 137 files, report "✓ Ingest complete" and write a manifest attesting to the 137. `scan(root:)` remains as a bundles-only convenience for callers that don't branch; **use `scanOutcome` anywhere the answer drives an exit code, an eject, or a claim of completeness.** An incomplete scan suppresses the eject in both the app (`IngestPlanner.scanWasComplete`) and the CLI.
3. **`ExifReader`** — `DateTimeOriginal` (or `Digitized`) via ImageIO, parsed in the *local* timezone; **for movies, AVFoundation's `creationDate`** (which, unlike an EXIF string, carries a real offset); falls back to file mtime. This date drives all folder grouping. It also returns the camera **model and body serial** from the same properties dictionary — one `CGImageSourceCopyPropertiesAtIndex` call, no extra I/O — which feed the `{CameraModel}` / `{BodySerial}` naming tokens.
4. **`PathPlanner.plan`** — groups bundles into `[YearGroup] → [DestinationFolder]` by capture date, for the preview tree. **`CopyPlan.plan`** — computes the actual destination path/filename per bundle.
   - The preview has two modes (toolbar Tree/Grid toggle, `PreviewMode`): the `PreviewTree`, and the **`ContactSheet`** culling grid. The contact sheet shows a thumbnail per bundle (`ThumbnailLoader`, an `actor` over ImageIO embedded-preview extraction with an in-memory cache) and lets the user deselect shots/days before ingest. Selection is tracked as `deselectedIDs: Set<AssetBundle.ID>` in `MainView` (empty = all selected; reset when the card changes); `startIngest` filters `yearGroups` to the selected bundles via `selectedYearGroups()`, and `canStartIngest` requires ≥1 selected.
5. **`IngestPlanner`** — orchestrates the scan (off-main via `Task.detached`) and holds `yearGroups` for the UI.
6. **`Copier`** — the copy engine (details below).

### The `AssetBundle` is the atomic unit

A bundle is one primary photo + its companions (`.xmp`/`.dop`/`.pp3` sidecars, JPEG pair, `.wav` audio note). Copy, verify, rollback, and dedup all operate on whole bundles: a RAW without its `.dop` has lost its edits, so they must move or fail together. `CompanionKind` is a deliberately closed set ([AssetBundle.swift](Sources/PhotoDropMac/Core/AssetBundle.swift)).

### Destination layout

`CopyPlan` writes to: `{root}[/{yyyy}]/{day-folder}/{filename}.{ext}`, where the **day-folder** and **filename** are user-configurable templates ([NamingTemplate.swift](Sources/PhotoDropMac/Core/NamingTemplate.swift)) and the year level is `NamingTemplate.yearFolder` (default **on**). The original extension is always re-appended. **The year used to be unconditional**, which made the most obvious layout of all — `{root}/2026-05-28/IMG_0001.jpg` — inexpressible however the templates were written; it defaults to on so an existing library keeps landing exactly where it always has. With it off, depth is entirely the template author's business. Named starting points live in [FolderLayout.swift](Sources/PhotoDropMac/Core/FolderLayout.swift) — four built-ins, **layout only**: they set the two templates and the year level and say nothing about *where* to save, which is how people describe this ("choose a folder, then put files in `YYYY-MM-DD/` under it"). `IngestPreset` remains the other thing: destinations + options + templates, for switching a whole configuration. Both the inspector's **Layout** picker and Settings → Naming call one `FolderLayout.apply(to:)` — two copies of "set these three keys" is how two surfaces end up disagreeing about what a layout means — and both *derive* the current selection with `FolderLayout.matching`, so no stored "which layout" key can contradict the templates it claims to describe. Defaults reproduce PhotoDrop's established pattern `{yyyy-MM-dd}[_{Description}]` / `{yyyyMMdd_HHmmss}_{OriginalStem}`. Template syntax: `{…}` tokens (a named token — `Description`, `OriginalName`, `OriginalStem`, `CardLabel`, `CameraModel`, `BodySerial`, `Sequence` / `Sequence:<width>` — or otherwise a Unicode date-format pattern applied to the capture date) and `[…]` optional groups (dropped when a named token inside renders empty). A literal `/` in the **folder** template nests subfolders below the year (e.g. `{MM}/{yyyy-MM-dd}` → `{root}/{yyyy}/05/2026-05-28`); the rendered output is split into components by `PathPlanner.sanitizedComponents`, each sanitized independently. User data (description/card label) is slash-stripped by `sanitize` *before* interpolation, so only the template author can nest. **`{Sequence}` is a filename token only, and renders empty in a folder template.** This is the preview/copy agreement rule, not an oversight: `PathPlanner.plan` renders the folder template to group the preview while `CopyPlan.destinationDirectory` renders it to place the bytes, and a sequence number cannot agree across them — the preview is planned over *every* bundle on the card and the copy runs over the *selected* ones, so a cull would shift every folder name out from under the tree the user just approved. `{CameraModel}` / `{BodySerial}` exist because two bodies covering one event both write `IMG_0001.CR2` and both fire at the same second: `CopyPlan` correctly pushes the second to `_1` and nothing is overwritten, but the suffix is assigned by **card ingest order**, so the frames end up indistinguishable by name and the same photo can carry different names in two libraries ingested in a different order. The intended escape hatch was `{CardLabel}`, and cards ship and reformat as `UNTITLED`.

`TemplateRenderer` is pure; **`PathPlanner.plan` groups the preview by the rendered day-folder path and `CopyPlan.destinationDirectory` renders the same components — they must agree**, including on the year level; `DestinationFolder.relativePath` *is* its `id`, so two folders are the same folder exactly when they land in the same place. The year stays part of the grouping key even when it is not a level, or two same-named day folders from different years would merge into one preview row. Companions are renamed to track the primary's new name so the stem relationship survives (long-form `IMG.DNG.xmp` → `{newName}.xmp`; short-form shared-stem → `{newStem}.{ext}`). Folder/description/template-output sanitization is in `PathPlanner.sanitize` (single component) / `PathPlanner.sanitizedComponents` (nested folder path). Composing a *filename* goes through **`PathPlanner.fileName(stem:extension:)`**, which caps stem+`.`+ext together: `sanitize` caps the stem alone, and a 255-byte stem plus `.CR2` is 259 bytes, which `open()` answers with `ENAMETOOLONG` — failing the copy and rolling back the bundle over a name.

**The plan is computed once and rebased onto every mirror** (`IngestEngine.rebase`), so a file has the same relative path at every destination. Planning per root let the `_1` disambiguator be chosen independently, and since the manifest records only the primary's path, `heal` would look for that name under a mirror root, miss, and call a file unrecoverable with a perfect copy present. Collision avoidance therefore unions the existing names across *all* destinations — a name is only free if it is free everywhere.

### The copy engine ([IngestEngine.swift](Sources/PhotoDropMac/Core/IngestEngine.swift))

**Everything below is `IngestEngine` (~560 lines), not `Copier`.** This section
used to be titled "`Copier`" and attributed the destination index, the rollback and the manifest writing to
it; `Copier` ([Copier.swift](Sources/PhotoDropMac/UI/Copier.swift)) is ~230 lines of main-actor state plumbing —
it owns the `CopierState`, the visible log, the cancellation generation and the terminal-state receipt, spawns
the engine detached, and marshals progress back. The engine is nonisolated, has no UI dependencies, and is what
the CLI drives directly. Sending a reader to the wrong file is the whole reason this correction is written out.

`Copier.start(...)` spawns a `Task` that runs `IngestEngine.run()`, which:
- Builds a **`DestinationIndex`** per destination root (size-bucketed snapshot; hashes only on a size collision) for dedup.
- Per bundle, per file: dedup-check → copy+tee-hash → optional verify → record hash. Skipped (duplicate) bytes still count toward progress so the bar fills smoothly.
- **Mirrors copy from the primary, not from the card.** The destination loop sits outside the copy and every pass used to read `file.source`, so a 3-destination job read the whole card **three times**, serially — roughly 4¼ hours for a 512 GB CFexpress that takes 85 minutes to read once, on reader hardware that heats and throttles. The headline 3-2-1 feature was the slowest path in the app. Each bundle records where its files landed under the primary (`primaryLanded`) and what the card's bytes hashed to (`primaryDigest`); mirrors read from there and their tee-hash is **compared against the card-side digest**, so a primary that no longer holds what came off the card halts the job at the first mirror. The old scheme hashed three independent reads and never compared them to each other, so it was strictly weaker. Falls back to the card in the two cases where the primary has nothing to offer — the primary pass itself, and a primary that failed and rolled back, which must not take its mirrors down with it. A dedup-skipped file's existing copy is used as a read source too; it is never in `writtenFiles`, so rollback cannot delete it out from under a mirror.
- **Source timestamps are preserved** (`futimens` + `ATTR_CMN_CRTIME`, best-effort). Nothing set them, so every file in the library wore the *ingest* time as both mtime and birth time — wrong on its own terms, and it destroyed the very mtime `ExifReader` falls back to, so re-ingesting an already-ingested folder filed everything under the copy date with no way back.
- **The job holds an idle-sleep assertion** ([PowerAssertion.swift](Sources/PhotoDropMac/Core/PowerAssertion.swift)) for its whole length, tail included. Nothing did, and the default battery idle timer is ten minutes while an offload is routinely longer: the Mac slept, a bus-powered reader lost power, and every remaining read failed with nobody having touched the machine. It prevents *idle* sleep only — an assertion that could defeat a lid close would be a way for a background app to cook a laptop in a bag — so the source-gone halt below covers the rest.
- **The source going away halts the job**, rather than failing every remaining bundle identically. A bumped reader at bundle 100 of 2000 produced 1,900 "could not be opened" lines — 5,700 with mirrors — and no statement of what had happened. On the first source-side `FileCopierError` the engine stats the mount point (`isSourceSide` compares *path components*, never a string prefix, for the reason `DestinationTopology` spells out) and stops if it is gone.
- **Rollback:** if any file in a bundle fails, already-written files from *that bundle* are deleted (the bundle is all-or-nothing).
- **Halt vs. continue:** a verification mismatch halts the entire job; any other per-bundle error is logged, counted as failed, and the job continues.
- **Destinations fail independently.** Each root gets its own `do/catch` inside the bundle loop, so a mirror that fails neither skips the mirrors after it nor voids the primary copy that already landed. `CopyResult.failuresByDestination` breaks the count down per root; `primaryFailures` / `failedMirrors` are what the UI keys on, so "the NAS was offline" no longer renders as a failed job. Do not re-wrap the destination loop in a single `do`.
- **Destination roots are deduped by filesystem identity** (`ArchiveDestinations.dedupedRoots`, dev+inode). One folder listed twice means pass 2 collides with pass 1's own file, `O_EXCL` correctly refuses, and a *successful* ingest reports every bundle failed. The settings and CLI layers dedupe too; the engine does it again because it takes roots from any caller.
- **Overlapping trees are refused, not warned about** ([DestinationTopology.swift](Sources/PhotoDropMac/Core/DestinationTopology.swift)). `dedupedRoots` catches roots that are the *same* folder; this catches roots that *contain* one another, and a destination that contains the source. `DestinationIndex.findDuplicate` matches content anywhere under a root by design, so any overlap makes files match copies of themselves and the job reads as a clean all-duplicate run — measured: `filesCopied == 0`, `filesFailed == 0`, no halt, a manifest pointing at the *source* files, and then an ejected card. Checked in `IngestEngine.run` (so app and CLI both inherit it), again in `PreflightCheck` for the GUI, and again in the CLI, which never calls `PreflightCheck`. Containment compares **path components** of the symlink-resolved paths, never a string prefix — `/Vol/Library2` is not inside `/Vol/Library`. **Symlinks are resolved on the deepest *existing* ancestor and the missing components re-appended**, because `resolvingSymlinksInPath()` consults the filesystem and so returns a different shape for a path that exists than for one that doesn't (`/private/tmp/x` → `/tmp/x` only when it exists). Comparing the two shapes finds no common prefix: measured, a nested archive root **that had not been created yet** — the unplugged-drive case `ArchiveDestinations.identity` documents as routine — was not detected as nested, and `heal --script <lib>/x.sh` wrote into the library it had just promised not to touch.
- **Collision keys are case- and NFC-folded** (`CopyPlan.collisionKey`). APFS is case-insensitive by default, so `IMG_0001.JPG` and `img_0001.jpg` are one file on disk; comparing raw strings meant `_1` never fired, `O_EXCL` refused the second write, and that bundle failed and rolled back on *every* run, forever. Folded unconditionally rather than probing each volume: the union rule below already requires a name to be free everywhere, so one case-insensitive mirror makes folding mandatory. Cost is an occasional unnecessary `_1`, never a lost file. `CopyPlan.separateInternalCollisions` handles the collisions *inside* one bundle, which `isTaken` structurally cannot see and the `_n` loop cannot resolve (companions follow the primary's stem, so they move together) — it must stay outside that loop or it spins forever.
- **The auto-eject is gated on `haltReason == nil && !wasCancelled && filesFailed == 0 && manifestFailures.isEmpty`**, runs **after** the manifest, the log and the volume flush, and the callers additionally refuse to eject after an incomplete scan *or a cull*. Three separate holes were here. The eject ran **before** the receipt that justifies it, so nothing after it could be recovered by re-reading a card that was already out. A failed manifest write was invisible — `ManifestWriter.write` answers `nil` on four distinct failures into a variable nothing checked, so a job could copy and verify every file, report `✓ Ingest complete`, exit 0 and eject over a library nothing can ever verify; it now lands in `CopyResult.manifestFailures`, which gates the eject, exits the CLI 1, and takes the seal off the completion sheet. And a **cull** is a partial view: deselected bundles are filtered out before the engine sees them, so `filesFailed` is honestly 0 and the engine's own gate opens while the frames the user deselected are still on the card. Ejecting is the one irreversible act in the job; a run that lost files to a full disk or a permission error used to eject anyway, putting the only remaining copy out of reach.
- **Child processes are run through [`ChildProcess`](Sources/PhotoDropMac/Core/ChildProcess.swift), which drains stdout and stderr *while* the child runs.** A pipe holds ~64 KiB; read it only after the child exits and any child that prints more than that blocks in `write()` and never exits. `PostIngestHook` did exactly that and deadlocked forever on a hook running `rsync -v` — measured: ~1 MiB of stdout never returned. `DriveEjector` and `ScheduledVerification` share the runner (they were safe only because `diskutil` and `launchctl` are quiet). Don't hand-roll a `Process` with an undrained `Pipe`. **Draining is not bounding**, so the runner is also bounded in all three directions: a `timeout` (default 5 min; `PostIngestHook` raises it to an hour) that SIGTERMs then SIGKILLs and sets `Output.timedOut` — a hook on a dropped mount never returned, and `Copier` spawns it detached, so that was one leaked task per ingest; a `maxOutputBytes` cap (8 MiB) past which bytes are read and *dropped* — reading can never stop early or the child blocks in `write()` again; and stdin wired to `/dev/null`, because an inherited descriptor leaves a hook that reads input waiting on something nobody can type into.
- **The volume barrier runs after the manifest, not before it.** `F_FULLFSYNC` per destination root used to be issued before the manifest was written, so the photos were durable and the receipt for them was not: `.atomic` gives rename-atomicity, not durability, and after `rename(2)` returns both the data and the directory entry can still be in the page cache — a power loss in the seconds after a job left a good library with `verify` reporting exit 2 "no manifest found" over it forever. The old `filesCopied > 0` gate is gone for the same reason: an all-duplicate re-ingest copies nothing and still writes a manifest. A volume that refuses the barrier is now named in the warning rather than reported anonymously once per job, which is how a user with one SMB mirror learned to ignore it.

**Cancelling still writes the manifest and log** for the bundles that completed, marked `partial: true`. Those files are on disk and are deliberately *not* rolled back, so returning early left them with no integrity record — and a later re-ingest dedup-skips them without a digest (`xxhash64: nil`), so `verify` would then report success over zero files. `run()` therefore always returns a `CopyResult`; `cancelled` distinguishes it. A cancelled job skips the eject.
- **Failure detail survives the job.** `CopyResult.failedFiles` records which bundles failed, at which destination, and why (capped at `maxRecordedFailures`; `failuresByDestination` remains the authority on the count). It carried counts alone, and the reasons existed only as log lines — which the detail pane stops rendering the moment a job leaves `.running` and `Copier.reset()` empties on dismiss, so the whole in-app account of a run that lost 40 of 500 files was *"40 files failed — see log for details"* and dismissing the sheet destroyed it. Rendered by [FailureList.swift](Sources/PhotoDropMac/UI/FailureList.swift), with a Copy Details button.
- **"Already there" says *where*.** `CopyResult.duplicatesFoundElsewhere` names the folders under the primary that already held this card's photos when they are **not** the folders this job planned. Dedup matches content anywhere under the root by design, so the likeliest shoot-day misstep — ingest with the description blank, realise you wanted "Smith Wedding", type it, ingest again — matched every file, copied nothing, never created the new folder, and reported *"Everything was already there — nothing new to copy."* The user reads that as done and reformats the card. This is a reporting fix; do **not** weaken `findDuplicate`.
- Optionally ejects the card (`DriveEjector` → `diskutil eject`), writes a log file (`JobLogger` → `~/Library/Logs/PhotoDrop/ingest-<ts>.log`), writes a **verification manifest** (`ManifestWriter` → `<primaryRoot>/PhotoDrop Manifests/ingest-<ts>.{json,csv}`), and persists the hash cache.

### Verification manifest (`Manifest` / `ManifestWriter`)

Every ingest writes a receipt of what landed and its checksums, as JSON + CSV + **MHL**, into a `PhotoDrop Manifests/` folder at **every** destination root (filename stamp matches the job's log). Entries are accumulated **per destination** (`manifestEntriesByRoot`, indexed by the `destination:` parameter of `copyBundle`) and cover copied/verified files and skipped duplicates alike, each with its `xxhash64`.

Writing only at the primary — on the reasoning that a mirror is byte-identical — meant a mirror had **no integrity record and no way to acquire one**: `verify <mirror>` exited 2 "no manifest found", and `verify --xattr` enumerates the files that *are* present, so it cannot detect absence (and no-ops entirely on exFAT/SMB, where `setxattr` fails). A 3-destination job whose NAS dropped offline at bundle 40 of 500 wrote one manifest recording `partial: false`, `destinations: [lib, nas, ssd]` and 500 verified entries, and every later `verify` and `heal` agreed with it forever. Each root's manifest now lists exactly what landed *there*, by construction, sets `primaryDestination` to its own root (so `heal` reading a mirror treats it as the library and the other roots as recovery sources), and derives its counts from its own entries. **`partial` is set by `wasCancelled || haltReason != nil || failuresByRoot[d] > 0`** — it consulted only cancel/halt, so a job that lost files still claimed completeness in the durable record. A failed `FileChecksumXattr.stamp` is logged **once per root** rather than discarded, since "this whole mirror carries no checksums" was otherwise indistinguishable from success. A skipped entry records the digest the dedup match was *made* on — it costs nothing, since `DestinationIndex.findDuplicate` computed it to decide the match, and recording `nil` there meant `VerifyEngine` skipped the entry, so a re-ingest of an already-complete card wrote a manifest that attested to nothing and `verify` reported success over zero files. `ManifestWriter.decode` also **validates `schema`**; it was written and never read, so any structurally-valid JSON was accepted as a manifest. The manifest URL flows back in `CopyResult.manifestURL`; the completion sheet's **Export Manifest…** saves a copy elsewhere. This surfaces the hashes the copy engine already computes rather than discarding them after the verify step.

**MHL is the interchange copy** ([MHLWriter.swift](Sources/PhotoDropMac/Core/MHLWriter.swift)) — the ASC-blessed XML that Hedge, OffShoot, Silverstack and ShotPut Pro all emit and that post houses and delivery specs require. The JSON/CSV pair could be read only by PhotoDrop, and a receipt nobody else can read is a receipt you cannot hand to anyone; that became load-bearing the moment video became ingestable. It is **secondary and best-effort, like the checksum xattr**: never read back — `verify`, `heal` and `sync` all derive from the JSON, which is where the schema check and the trust rules live — and a failure to write it never fails a job. The digest is the manifest's own `%016llx`, which already *is* the big-endian XXH64 that MHL's `<xxhash64be>` names, so nothing is recomputed and the two records cannot disagree. Entries with no digest are **omitted**, not written with an empty hash, since an MHL entry exists to assert a checksum. Filenames are XML-escaped — a name is untrusted text and this is a sink that renders it, the same category as `CLIOutput.safe`, `JobLogger` and `HealEngine.comment`.

Manifests are named `<kind>-<stamp>` where kind is `ingest` or `sync`; a sync record filed under `ingest-` would claim a card was read when none was.

**The job stamp is shared and sub-second** ([JobStamp.swift](Sources/PhotoDropMac/Core/JobStamp.swift)). A job's two artifacts — the manifest and the log — are named `ingest-<stamp>` and are paired by that name, so the format lives in `JobStamp.fileStamp` rather than being written out at both call sites, where it had already been duplicated and could drift apart unnoticed. Use it for anything else named per-job; don't re-declare the format.

**Names are claimed, not just stamped.** Every per-job artifact goes through `JobStamp.claimUniqueName`, which creates the file with `O_CREAT | O_EXCL` and appends `-2`, `-3`, … only on an actual collision. This is the part that makes overwriting *impossible*; the timestamp precision only makes collisions rarer. Do not replace it with a `fileExists` check — that is a TOCTOU, and `.atomic` writes replace the loser.

Why it matters: `ManifestWriter` writes `.atomic`, so two jobs sharing a name means the second silently destroys the first's manifest — and a lost manifest doesn't fail loudly. `verify` reports success over whatever records remain, printing `✓ All 1 file …` for a two-file library and exiting 0. Measured before the claim existed: ten concurrent `photodrop ingest` processes into one library copied all ten files but left **two** manifests. Process launches cluster inside a millisecond, so precision alone bought almost nothing there.

The precision (`yyyyMMdd-HHmmss-SSS`) still earns its place — it keeps ordinary runs collision-free and therefore suffix-free, so filenames stay clean and sort chronologically. Milliseconds are separated with `-`, not `.`, so the stamp can never be read as a file extension.

The CSV takes the JSON's *resolved* base and the log takes the manifest's, so a suffixed manifest is never paired with an unsuffixed sibling. Pairing is nonetheless best-effort: manifests live per-destination while logs share one global folder, so concurrent jobs to *different* libraries can collide in the log folder and not the manifest folder.

**Per-file checksum xattr.** In addition to the manifest, `IngestEngine` stamps each copied file's `xxhash64` into an extended attribute (`com.tsvb.photodrop.xxh64`, [FileChecksumXattr.swift](Sources/PhotoDropMac/Core/FileChecksumXattr.swift)) so the file carries its own checksum. `VerifyEngine.runXattr` (CLI `verify --xattr`) walks a folder and re-checks every stamped file — manifest-free, so it survives a library reorg or a lost manifest. **Secondary and best-effort**: xattrs are stripped by exFAT/FAT, some cloud sync, and `cp -X`, so the manifest stays authoritative; an absent attribute means "unstamped", never "changed".

`VerifyReport` also carries **`undigested`, `outOfRoot` and `partialManifests`** — the manifest-mode counterparts, all of which were previously invisible. `build` dropped entries with no usable digest and entries whose path failed containment with a bare `continue`, and `total` is `verified + issues.count`, so a library whose manifests are mostly older skip entries (`xxhash64: nil`) verified the remainder and printed a clean tick; a manifest half full of traversal attempts read as a pass over the half that wasn't. **`partial` was written on every job and read by nothing** — cancel a 500-photo ingest at bundle 40 and the manifest correctly said `partial: true` with 40 entries, and `verify` answered `✓ All 40 files match`, exit 0, with the nightly agent green forever over a library known to be incomplete. All three are *reported, never failed on*: the files that landed really are intact, and the incompleteness is context the user must be told rather than an integrity error. (`build` returns a named `Plan` struct rather than a tuple for exactly this reason — an unnamed tuple member is easy to drop with `_` at a call site, which is how these went missing.)

`VerifyReport` therefore carries **`unreadableDirectories` and `unstamped`** — what the run could not examine. The walk had no `errorHandler`, so a mode-0000 or dropped-NAS subtree was skipped in silence and printed `✓ All 4,200 files match` with exit 0 over a library a third of which was never opened; and unstamped files were dropped with no tally, so a library that lost 9,000 of 10,000 xattrs read as a pass over the surviving 1,000. `unreadableDirectories > 0` makes `allGood` false (exit 1); `unstamped` is always *reported* but never fails on its own — that would contradict "an absent attribute means unstamped" and make every partially-stamped library cry wolf nightly. A run where **nothing** could be checked and files were present exits 1 rather than printing "no checksummed files found". Files under `PhotoDrop Manifests/` are excluded from the tally: they are the record, not library content, and counting them put a permanent floor of 2 on every clean report.

**Re-verification** (`Verifier` / `VerifySheet`, launched from the toolbar's *Verify Library* button): pick a library folder (or a manifest `.json`) and it re-hashes every file recorded in the manifest(s) under `<folder>/PhotoDrop Manifests/`, reporting matches / changed (silent corruption) / missing / conflicting. The manifest is the source of truth for *what to check* — each file resolves relative to its own manifest's location (two levels up), so a moved library still verifies. Hashing reuses `XxHash64.hash(fileAt:)` and runs off-main with throttled progress, mirroring the copy engine.

### The manifest is untrusted input

A manifest is unauthenticated data sitting inside the tree it attests to, and `verify`/`heal` accept a target the user did not necessarily produce — a shared or downloaded library, a folder on the card, or a `.json` handed straight to the CLI (whose library root is then taken as two levels up from that file). Several rules follow, and each is load-bearing:

- **Every manifest-derived path goes through `ManifestWriter.resolve(entryPath:under:)`**, which drops anything resolving outside the library root. Without it a `../../..` entry makes verify hash arbitrary files (an existence/content oracle) and makes `heal --script` emit a `cp` that overwrites them. The check is **lexical** on purpose: `standardizedFileURL` consults the filesystem and so returns a different shape for paths that exist than for ones that don't, which would silently drop every missing file — exactly what `heal` looks for. **Reports name the resolved path, via `ManifestWriter.relativePath(of:under:)`, never the manifest's string.** An absolute entry is deliberately re-rooted rather than honored, so a manifest naming `/etc/hosts` made `verify` print `MISSING /etc/hosts` — a claim about a system file it never touched. The verdict line is this app's whole product; it may only name paths it inspected.
- **Manifests that disagree are reported, never reconciled.** Two manifests recording the same digest for a path dedupe quietly (a re-ingest re-records what it skipped); two recording *different* digests produce a `.conflict` issue. There is no trustworthy tiebreak — `createdAt`, the `ingest-<stamp>` filename, and the file's mtime are all chosen by whoever wrote the file, so a "newest wins" rule let a planted manifest relabel a tampered file as verified.
- **`heal` derives what to expect of a file from `VerifyEngine.build`, never its own merge.** Both engines read the same unauthenticated manifests, so they must apply the same trust rule — and `build` is where that rule is stated and argued. `HealEngine` used to keep a private oldest→newest merge in which the newest manifest won; planting one JSON dated 2099 that recorded a tampered file's *current* digest made `heal` call the corrupted library healthy while `verify` correctly reported the conflict. Conflicted files surface as `HealCandidate.Kind.conflicted` and are never recoverable — offering a restore would mean picking a winner among disagreeing records. Extend `build` if heal needs more; don't reintroduce a second merge.
- **A recorded mirror root is not trusted just because a manifest names it.** `VerifyEngine.build` searches a `destinations[]` root only if it carries a `PhotoDrop Manifests/` folder (every root a job writes gets one) or the user named it — `photodrop heal --mirror <path>`, the escape hatch for mirrors written before mirrors carried manifests. Without the gate, one planted JSON turned `heal` into a hash-equality oracle over every directory the user can read: reproduced with a synthetic attacker root and a mode-600 dummy, heal hashed it, named it as the restore source, and `--script` emitted a `cp` from it. Refused roots are **reported**, never silently dropped — "we didn't look there" is how a recovery tool lies by omission.
- **`XxHash64.hash(fileAt:)` refuses anything that isn't a regular file, and checks before it opens.** Containment proves only that an entry stayed inside its *recorded* root, and a recorded root can be anywhere: `"destinations": ["<lib>", "/dev"]` plus an entry `zero` had `heal` read `/dev/zero` — measured still running at 12 s and 1,896 MB RSS. `open()` on a FIFO blocks until a writer appears, so an `fstat` on the descriptor would never run; the descriptor is re-checked afterwards anyway. The read loop also drains an `autoreleasepool` per chunk — with no attacker at all, verifying one 3 GiB file peaked at 1,876 MiB RSS.
- **`heal` stays report-only, and the script it emits must stay reviewable.** `restoreScript` lists the source roots it will copy *from* (they come from the manifest's `destinations`, which can name anywhere) and refuses to emit an executable line for a path that `SafeText.containsDangerousControls` flags — such a path is quoted correctly but renders across several lines, so an embedded `rm -rf $HOME` reads like a command to whoever is reviewing. That predicate covers **bidi and invisible formatting as well as C0/DEL**: those carry no control semantics at all, they reorder the text around them, so quoting is correct and useless — measured, a filename holding U+202E earned a live `mkdir -p … && cp -p …` line. Both classes survive `PathPlanner.sanitize`, which trims only the ends, so both are reachable from a card filename.

**Untrusted text is neutralized at every sink that renders it, from one definition.** [SafeText.swift](Sources/PhotoDropMac/Core/SafeText.swift) holds both rules — the escaper and the predicate above — and is used by `CLIOutput.safe` (terminal), `JobLogger` (the on-disk log the user reads afterwards) and `HealEngine.comment` (shell comments). The log was the sink nobody guarded: confirmed against a real log, a file named `IMG\x1B[2K\x1B[1A0002.CR2` erased the preceding VERIFY line when the log was `cat`ed, and a raw newline forged an entire fabricated record. **Storage is deliberately not sanitized** — the manifest and the filesystem must record the name the file actually has, or verification stops meaning anything.

Relatedly, `AssetDiscovery`'s `isRegularFile` guard is a security boundary, not a directory filter: it has lstat semantics, so it is what stops a symlink on the card from pulling `/etc` or `~/.ssh` into the library. `HashCacheEntry.matches` is likewise strict (exact nanosecond mtime + birth time) because a false cache *hit* silently skips a photo as a duplicate, and skipped entries carry no digest for a later verify to catch.

State is exposed as a `CopierState` enum (`idle`/`running`/`completed`/`cancelled`/`failed`) that the UI switches on.

### Hashing & dedup performance model

- **`XxHash64`** ([Hasher.swift](Sources/PhotoDropMac/Core/Hasher.swift)) — a pure-Swift, value-type (trivially `Sendable`) streaming XXH64. Non-cryptographic; used only for copy verification and dedup equality.
- **Tee-hashing** (`FileCopier.copyAndHash`) — streams source→dest in 1 MiB chunks while hashing the bytes in flight, so a multi-GB file is read exactly once for both the copy and its digest.
- **`HashCache`** (an `actor`, persisted as JSON at `~/Library/Application Support/PhotoDropMac/hash-cache.json`) — caches digests keyed by `volumeUUID|path` (source) / `path` (dest), validated by `(size, mtime)`. A warm cache turns a re-ingest of the same card against the same destination from a file-read-bound operation into a stat-bound one (the SD read, the bottleneck, is skipped entirely). After a copy+verify, the just-computed digest is written back via `recordDestination` so the next run doesn't re-hash it. **`init` does no I/O** — an actor's init body runs synchronously on the caller, and `Copier.init` is constructed from `MainView`'s `@State`, so the whole cache was being read and JSON-decoded on the main thread at every window open; loading now happens inside the actor on first use. The maps are **pruned on save** to `maxEntriesPerMap`, dropping entries whose file no longer matches first (they can never hit again) and then the oldest — the cache had no eviction at all and grew for the life of the install. Both are safe for the same reason any miss is: every hit is re-validated against `stat`, so a dropped entry costs one re-hash and can never yield a wrong digest.
- **Dedup semantics** (`DestinationIndex.findDuplicate`) are content-based: a file is a duplicate if size+hash match **anywhere under the destination root**, not just at the same path — so a renamed earlier import is still detected. **The match is then confirmed against the candidate's real bytes (`bypassCache: true`) before the copy is skipped.** `HashCache.destinationHash` answers from the persisted cache whenever `stat` still agrees on (size, mtime, birthtime), so on a warm cache *nothing had read the file the skip vouches for* — and silent corruption (bit rot, a bad cable, a partial restore) changes content without touching any of those three. A file that rotted after its ingest therefore matched its own stale digest, the copy was skipped, and a **fresh** manifest entry recorded that digest for that path; the user then wiped the card. The comment on `fullWalk` cites this re-read as the reason that walk may skip directories safely, so it has to actually happen. Cost is one device read per *actual* skip, not per candidate — the cache still narrows a size collision to one file first, and the card side is untouched. A disagreeing entry is dropped via `HashCache.invalidateDestination` rather than left to make the same wrong call next run.

## Concurrency conventions

Swift 6 strict concurrency is on. Follow the existing split:
- UI-facing state lives in `@MainActor @Observable final class` controllers (`DriveWatcher`, `IngestPlanner`, `Copier`). `@ObservationIgnored` marks internal bookkeeping that shouldn't trigger view updates.
- Heavy/blocking work (directory scans, hashing, copying, `DestinationIndex.build`) runs on `Task.detached(priority: .userInitiated)`; results hop back to the main actor.
- Progress from the detached copy is throttled (~0.1 s) and marshalled back via `MainActor` hops (`addBytesCopied`) to avoid flooding the UI.
- Shared mutable cross-task state uses an `actor` (`HashCache`). Domain models are value types and therefore `Sendable`.

## Settings contract (`@AppStorage`)

Preferences are plain `@AppStorage` keys with **no central store**, and the same key is re-declared at every
site that reads it. **Eleven files declare them** — [MainView.swift](Sources/PhotoDropMac/UI/MainView.swift),
[SettingsView.swift](Sources/PhotoDropMac/UI/SettingsView.swift),
[InspectorPane.swift](Sources/PhotoDropMac/UI/InspectorPane.swift),
[PhotoDropMacApp.swift](Sources/PhotoDropMac/UI/PhotoDropMacApp.swift),
[ProgressPane.swift](Sources/PhotoDropMac/UI/ProgressPane.swift),
[CompletionSheet.swift](Sources/PhotoDropMac/UI/CompletionSheet.swift),
[ContactSheet.swift](Sources/PhotoDropMac/UI/ContactSheet.swift),
[PreviewTree.swift](Sources/PhotoDropMac/UI/PreviewTree.swift),
[VerifySheet.swift](Sources/PhotoDropMac/UI/VerifySheet.swift),
[AppCoordinator.swift](Sources/PhotoDropMac/UI/AppCoordinator.swift) and
[IngestPreset.swift](Sources/PhotoDropMac/Core/IngestPreset.swift). `photodrop.verificationStyle` alone has nine
declarations across eight files. This doc previously said "three files", which undercut the very invariant it was
stating. If you add or rename a key, `grep` for it and update **every** declaration site or the views silently
desync:

- `photodrop.primaryDestination` (String)
- `photodrop.archiveDestination` (String, optional second copy)
- `photodrop.extraArchiveDestinations` (String, newline-separated extra archive folder paths) — edited through **one** shared `ExtraDestinationsEditor` in both the inspector and Settings → General. Settings used to offer a bare `TextEditor` over the raw string while the inspector had stable rows with a folder picker each, so the surface a user is likelier to find first was the one that made them hand-type filesystem paths, where a typo is invisible until it becomes a "failed mirror" line in a log — additional mirror destinations beyond Primary + Archive (3-2-1 backups). Assembled with the primary archive into the ordered list via [ArchiveDestinations.swift](Sources/PhotoDropMac/Core/ArchiveDestinations.swift); each gets its own verified copy, and each fails independently of the others. `list` takes the **primary** so it can drop any spelling of it — see the dedup note under the copy engine. Edited in Settings → General
- `photodrop.verifyCopies` (Bool, default `true`)
- `photodrop.ejectAfterIngest` (Bool, default `false`)
- `photodrop.showCompletionSheet` (Bool, default `true`)
- `photodrop.notify.authorizationDenied` (Bool, default `false`) — written by [Notifier.swift](Sources/PhotoDropMac/UI/Notifier.swift), not by a toggle: macOS refused permission to post banners. Authorization is requested when the user turns notifications *on* (in Settings, where they can answer the prompt) rather than lazily from `post`, which by construction ran while the app was **not** frontmost and discarded the answer — leaving a checked toggle that never produced a banner. Settings shows a warning row when this is set
- `photodrop.notifyOnCompletion` (Bool, default `true`) — posts a Notification Center banner on finish when the app isn't frontmost ([Notifier.swift](Sources/PhotoDropMac/UI/Notifier.swift), read via `UserDefaults`; toggled in `SettingsView`)
- `photodrop.postIngestScript` (String, default empty) — path to an executable run after a clean (non-halted) ingest ([PostIngestHook.swift](Sources/PhotoDropMac/Core/PostIngestHook.swift)); argv[1] is the primary destination, job details are in `PHOTODROP_*` env vars. Best-effort (a failure posts a banner, never affects the copy). Read via `UserDefaults` in `Copier`, set in `SettingsView`
- `photodrop.template.folder` (String, default `{yyyy-MM-dd}[_{Description}]`) — day-folder name template
- `photodrop.template.yearFolder` (Bool, default `true`) — whether a `{yyyy}` level sits above the day folder. Off gives `{root}/2026-05-28/IMG_0001.jpg`. Carried by `IngestPreset` too, decoded as `true` when absent so an older preset keeps the layout it was saved with
- `photodrop.template.filename` (String, default `{yyyyMMdd_HHmmss}_{OriginalStem}`) — primary file stem template (extension auto-appended). Both edited in Settings → Naming (`NamingPreferences`), read in `MainView` (threaded to `IngestPlanner`/`Copier`)
- `photodrop.verificationStyle` (`VerificationStyle` rawValue, default `.steady`) — the app **theme**, a committed identity per option (not just an accent):
  - `.steady` — the system look: your accent, your light/dark, SF Pro. The no-transformation default.
  - `.ledger` — editorial: forced **light**, **serif** type, **ultramarine** ink (`#120A8F`), wax-seal `StampMark`.
  - `.pressroom` — wire desk: forced **dark**, **monospaced** type, **marigold** accent, aperture-iris mark.

  Each theme's `accent` / `fontDesign` / `colorScheme` live in [VerificationStyle+Theme.swift](Sources/PhotoDropMac/UI/VerificationStyle+Theme.swift) and are applied together via `.tint()` / `.fontDesign()` / `.preferredColorScheme()` at the app root ([PhotoDropMacApp.swift](Sources/PhotoDropMac/UI/PhotoDropMacApp.swift)). `resolvedAccent` recolours the explicit-accent icons (`SidebarRow`, `FolderRow`) and the verification marks (`SealGrid`/`StampMark`/`ApertureMark` take an `accent:` param) that `.tint` doesn't reach. Mark/headline switches are inline in `ProgressPane`/`CompletionSheet`.
- `photodrop.cardHistory` (JSON, written by [CardHistory.swift](Sources/PhotoDropMac/Core/CardHistory.swift), not by a control) — which cards have been ingested, keyed on **volume UUID** rather than label, because cards ship and reformat as `UNTITLED` and a label collision would make two cards look like one. A wedding is six cards from three bodies plus a second shooter's at midnight; the app modelled six unrelated events and recorded nothing, so "did I already do this one?" had no answer short of re-inserting the card and waiting out a full index build and source-hash pass. Recorded only on a **clean** completion — a halted job leaves the card as unfinished business and a sidebar row claiming otherwise is worse than no row — and read through the injected `defaults`, so the test suite never touches `UserDefaults.standard`
- `photodrop.menuBar.visibility` (`MenuBarVisibility` rawValue, default `.always`)
- `photodrop.menuBar.autoOpenWindow` (Bool, default `true`)
- `photodrop.menuBar.oneClickIngest` (Bool, default `false`)
- `photodrop.scheduledVerify.enabled` / `.schedule` (`VerifySchedule` rawValue: daily/weekly/monthly) / `.binaryPath` (path to the built `photodrop` CLI) / `.library` (override; defaults to the primary destination) — Settings → Maintenance. Toggling installs/removes a `launchd` user agent ([ScheduledVerification.swift](Sources/PhotoDropMac/Core/ScheduledVerification.swift)) that runs `photodrop verify --json` at 03:00 on the chosen cadence and posts a notification (via `osascript`). Report-only; the unsandboxed app manages `~/Library/LaunchAgents` directly.

  **Exit 1 and exit 2 get different notifications** — "found issues" vs "could not verify (no manifest)". The old `verify … || osascript "found issues"` fired the same alarm for both, so an agent pointed at a library with no manifest cried wolf nightly, which is how a user learns to ignore it. Output is echoed only on a non-zero exit, so the log doesn't grow by a JSON report a night. `install` removes the plist again if `bootstrap` fails (an orphan plist left the toggle reading *off* while launchd could still load the job at next login), `isLoaded()` asks launchd rather than stat-ing the plist, and Settings re-applies when the binary or library path changes — the installed agent bakes those in, so editing them silently pointed the job at the old library. **Both halves of the agent are injectable** (`ScheduledVerification.Agent`: the `LaunchAgents` directory and the `launchctl` binary), for the same reason `JobLogger` takes a `directory:` — a test that installs a real login item is indistinguishable from the user doing it. Measured the hard way: before the injection existed, one red-phase run wrote and removed a plist in the developer's actual `~/Library/LaunchAgents` and invoked the real `launchctl`. CI now fails if that file appears after a test run

**Deliberate UI non-goals**, so nobody re-derives them as gaps: there is **no localization** (all strings are inline en-US) and **no `@SceneStorage`** (window state is not restored across launches). Both are defensible for a one-developer tool with one window; they are choices, not oversights. Accessibility is *not* on that list — image-only controls carry an `accessibilityLabel` as well as `.help()` (different AX attributes), the contact-sheet cells are exposed as toggling buttons **and are focusable with arrow/space/return key handling** (they were VoiceOver-reachable but inert to plain keyboard, so culling 2,000 frames meant 2,000 mouse clicks), and the toolbar actions have menu equivalents via `PhotoDropCommands` so the app is operable without a mouse. The Help menu is also populated now — macOS renders one whether or not anything is behind it, and ours opened nothing, which meant the shipped app named no way to reach the project, report a problem, or discover the CLI inside its own bundle (`AppLinks`).

The enum-typed keys (`VerificationStyle`, `MenuBarVisibility`) get their `String`-backed type from [AppCoordinator.swift](Sources/PhotoDropMac/UI/AppCoordinator.swift), which is the one definition site shared by every `@AppStorage` declaration.
